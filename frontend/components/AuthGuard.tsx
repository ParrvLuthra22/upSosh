'use client';
/**
 * components/AuthGuard.tsx
 * ─────────────────────────
 * Legacy shim — kept so existing imports compile.
 * New code should use ProtectedRoute or withAuth from @/components/ProtectedRoute.
 */
export { default } from '@/components/ProtectedRoute';
export { withAuth } from '@/components/ProtectedRoute';
