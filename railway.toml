[build]
builder = "NIXPACKS"

[build.nixpacksPlan]
phases.install = { cmd = "npm ci --workspace=backend" }
phases.build = { cmd = "cd backend && npm run build" }

[deploy]
startCommand = "cd backend && npm run start"
healthcheckPath = "/health"
healthcheckTimeout = 300
